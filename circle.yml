dependencies:
  pre:
    - pip install pre-commit

  override:
    - pre-commit install

    - pip install -r requirements.txt
    - pip install -e .

test:
  override:
    - pre-commit run --all-files
